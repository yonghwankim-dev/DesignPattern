package chapter02_객체지향원리.ch02_72p;

import java.util.ArrayList;

/**
 * 프로그래머의 의도는 ArrayList 클래스에 정의된 isEmpty, size, add, remove
 * 메서드를 자신이 구현하지 않고 그대로 사용하길 원함
 * 
 * 하지만 ArrayList 클래스에 정의된 스택과 관련없는 불필요한 연산까지 같이 상속받는 문제점
 * 이 발생한다.
 * 
 * 이는 아래와 같이 push나 pop 메서드를 통하지 않고 스택의 자료구조에 직접 접근이 가능해진다.
 * 이는 스택의 무결성 조건인 LIFO에 위배된다.
 * @author qkdlf
 *
 * @param <String>
 */
public class MyStack<String> extends ArrayList<String>{
	public void push(String element)
	{
		add(element);
	}
	
	public String pop()
	{
		return remove(size()-1);
	}
}
